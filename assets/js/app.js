const questions=[...document.querySelectorAll('.question')],progress=[...document.querySelectorAll('.progress span')],number=document.querySelector('.question-number'),result=document.querySelector('.checkin-result');let step=0;
questions.forEach((question,index)=>question.querySelectorAll('button').forEach(button=>button.addEventListener('click',()=>{question.querySelectorAll('button').forEach(b=>b.classList.remove('selected'));button.classList.add('selected');setTimeout(()=>{question.classList.remove('active');step=index+1;if(step<questions.length){questions[step].classList.add('active');number.textContent=`0${step+1} / 03`;progress[step].style.background='#172d2c'}else{number.style.display='none';result.classList.add('visible');progress.forEach(p=>p.style.background='#172d2c')}},260)})));
document.querySelector('.restart').addEventListener('click',()=>{result.classList.remove('visible');number.style.display='block';step=0;number.textContent='01 / 03';questions.forEach((q,i)=>{q.classList.toggle('active',i===0);q.querySelectorAll('button').forEach(b=>b.classList.remove('selected'))});progress.forEach((p,i)=>p.style.background=i?'#91a88e':'#172d2c')});
const toggle=document.querySelector('.nav-toggle'),nav=document.querySelector('.site-header nav');toggle.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')==='true';toggle.setAttribute('aria-expanded',!open);nav.style.display=open?'none':'flex';nav.style.cssText+=open?'':';position:absolute;top:70px;left:0;right:0;background:#f8f5ee;padding:20px 25px;z-index:20;flex-direction:column;gap:12px;box-shadow:0 8px 10px #0001'});
const breath=document.getElementById('breath-toggle'),art=document.querySelector('.tool-art'),word=document.getElementById('breath-word'),count=document.getElementById('breath-count');let timer,remaining=60;breath.addEventListener('click',()=>{if(timer){clearInterval(timer);timer=null;art.classList.remove('running');breath.textContent='Start a 60 sec reset';word.textContent='Breathe in';count.textContent='4';return}remaining=60;art.classList.add('running');breath.textContent='Pause reset';timer=setInterval(()=>{remaining--;count.textContent=remaining;word.textContent=remaining%8<4?'Breathe in':'Breathe out';if(!remaining){clearInterval(timer);timer=null;art.classList.remove('running');breath.textContent='Start another reset';word.textContent='A little lighter?';count.textContent='✦'}},1000)});
